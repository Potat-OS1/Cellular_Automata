package com.example.app.properties;

public interface Solid {
    //ill probably change this to state or something in the future, rn i just want a quick way to see if something is solid, so a blank interface seemed to work.
}
